export const DEFAULT_POLICY = {
  // fraction of claims in majority cluster required to release
  releaseThreshold: 0.75,
  // fraction required to escalate when less than releaseThreshold
  escalateThreshold: 0.4,
};
