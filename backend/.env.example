PORT=4000
CORTENSOR_API_KEY=your_cortensor_api_key_here
DATABASE_URL=postgres://user:pass@localhost:5432/inception

# Cortensor (on-chain SDK) — when using Phase 3.2
# Arbitrum Sepolia RPC (Alchemy)
CORTENSOR_RPC=https://arb-sepolia.g.alchemy.com/v2/WoBxG0GRpeeYmu4Lj5pfp

# Wallet that owns Session #141 (NEVER commit real private keys to git)
CORTENSOR_PRIVATE_KEY=0xd577523673002f563f5260feeecff7cc8044b849ded6743852a32585d9b43e83

# Cortensor contracts (from Discord / docs)
SESSION_V2_ADDRESS=0x2e9cC638CF07efdeC82b4beF932Ca4a8Dcd55015
SESSION_QUEUE_V2_ADDRESS=0x9a9Db957e106894D598bf3AD912F3B604C085235

# Optional: Cortensor Web2 submission API (useful when router isn't running)
CORTENSOR_WEB2_URL=https://db-be-testnet-0.cortensor.network
# Path template where {sessionId} will be replaced. Keep empty to use default as
# `/sessions/{sessionId}/tasks`.
CORTENSOR_WEB2_TASKS_PATH=/sessions/{sessionId}/tasks

# session id to use with example scripts
SESSION_ID=141

# Optional: E2E test gate (set to 1 to run end-to-end test)
E2E=0
